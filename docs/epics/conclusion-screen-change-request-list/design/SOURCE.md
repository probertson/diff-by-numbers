# Design source

No design artifact, by decision: the conclusion screen is plain text. Its content and line order
are fixed by the text layout in `breakdown.md` ("Conclusion screen layout"), and the footer copies
the existing Step footer (`internal/tui/tui.go:1084`).
